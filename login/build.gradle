apply from: '../module.build.gradle'
android {
    defaultConfig {
        if (isRunAlone.toBoolean()) {
            // 单独调试时添加 applicationId ，集成调试时移除
            applicationId "com.hongj.login"
        }
    }
    //统一资源前缀，规范资源引用
    resourcePrefix "login_"
}

